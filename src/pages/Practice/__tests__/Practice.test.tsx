import { describe, it } from "vitest";

describe("Practice", () => {
  it("should render the correct markup", () => {});

  it("should check the lesson on submit", () => {});

  it("should progress the lesson when submitted", () => {});
});
