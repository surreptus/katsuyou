import { describe, it } from "vitest";

describe("useLessons", () => {
  it("should source lessons from local storage", () => {});

  it("should use the first lesson as the default lesson", () => {});

  it("should generate a new lesson", () => {});

  it("should progress the lesson when completed correctly", () => {});
});
